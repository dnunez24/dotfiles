#!/usr/bin/env zsh
# shellcheck shell=bash

function ssh-passwd() {
  for key in ~/.ssh/id_*; do
    if [[ ! "$key" =~ \.pub$ ]]; then
      echo "Update password for $key"
      ssk-keygen -p -f "$key"
    fi
  done
}
