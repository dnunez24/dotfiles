- defaults:
    link:
      create: true
      # force: true
      relink: true

- clean:
  - '~'
  - ~/.config

- link:
    ~/.atom:
    ~/.config/fish:
    ~/.config/git:
    ~/.config/psysh:
      force: true
    ~/.dotfiles: ''
    ~/.gitconfig:
      force: true
    ~/.bin/git-open: git-open/git-open
    ~/.hyper.js:
    ~/.ssh/config:
      path: ssh/config
      force: true
    ~/.tmux.conf:

- shell:
  - [git submodule update --init --recursive, Installing git submodules]
  # TODO: handle local or system specific Homebrew packages
  # - [brew bundle, Install Homebrew packages]
  # - [apm install --packages-file=atom/packages.list --compatible --production, Installing Atom packages]
  # - [apm clean, Remove unspecifed Atom packages]
  # - [fish_update_completions, Update fish autocompletions]
